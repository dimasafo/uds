service http
{
       socket_type             = stream
       wait                    = no
       user                    = root
       server                  = /usr/local/bin/myweb_wrap 
       server_args	       = /usr/local/bin/myweb_folder/webroot /usr/local/bin/myweb_folder/log
}

